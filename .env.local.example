# OpenAI API 키 (이미지 생성 gpt-image-1 + 이야기 생성 gpt-4o-mini에 사용)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-...

# 메타(인스타·페이스북) 광고 픽셀 ID — business.facebook.com > 이벤트 관리자에서 발급.
# 비워두면 픽셀 스크립트를 아예 넣지 않는다 (로컬·프리뷰는 비워둘 것).
NEXT_PUBLIC_META_PIXEL_ID=
